{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Glob|Grep",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .hedwig-cg/knowledge.db ] && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"hedwig-cg: Code graph available. Use `hedwig-cg --json search \\\"<query>\\\"` (5-signal HybridRAG) instead of grepping raw files. This single command covers vector, graph, keyword, and community search with RRF fusion.\"}}' || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"$(python -c 'import hedwig_cg; import pathlib; print(pathlib.Path(hedwig_cg.__file__).parent / \"scripts\" / \"auto_rebuild.sh\")')\" 2>/dev/null || true",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
